apiVersion: v1
kind: Service
metadata:
  name: {{ include "renovate-server.fullname" . }}
  labels:
    {{- include "renovate-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "renovate-server.selectorLabels" . | nindent 4 }}
